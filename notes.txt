// Modular
a % b = x
if gcd(a,b)=k, then x is also a multiple of k

if a' is a multiple of a,
for various a' % b = k'
k' = [ k, 2k, 3k .... until less than b]

LCM doesn't follow mod property
LCM(a,b,c) != LCM(LCM(a,b)%mod, c)
Factorize and solve

// General
digital root of a number = num % 9 

Square root trick : Brute force for values <= sqrt(x)
Some optimization for values >= sqrt(x) to have sqrt(x) iterations